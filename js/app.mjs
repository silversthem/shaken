/*
    Contains the app's main features
*/

// html dom elements of the app


export class App {
    // Creates the app instance, loading all necessary components
    constructor() {
        this.tagList = new Set()
    }
    // Adds a tag to the taglist and updates the search
    addTag(tag) {

    }
    // Deletes a tag from the taglist and updates the search
    deleteTag(tag) {

    }
    // updates the search
    updateSearch() {

    }
    // focuses on a result
    focusResult(name) {

    }
    // unfocuses the focused result
    unfocusResult() {

    }
    // Starts the app
    serve() {
        // binding events to keyboard and autocomplete input
        // starting routing
    }
}